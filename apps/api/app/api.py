from fastapi import APIRouter
from app.config import settings

api_router = APIRouter()

# Register later endpoints here
# api_router.include_router(downloads.router, prefix="/downloads", tags=["downloads"])
# ...
